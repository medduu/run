version: 0.2

phases:
  install:
    runtime-versions:
      java: openjdk8
  build:
    commands:
    - mvn clean install
  post_build:
    commands:
    - echo $(basename ./target/*.jar)
    - pwd
artifacts:
  files:
  - target/messageUtil-1.0.jar
cache:
  paths:
  - '/root/.m2/**/*'