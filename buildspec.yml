version: 0.2

phases:
  install:
    runtime-versions:
      java: openjdk8
  build:
    commands:
    - chmod 777 ./.mvn/*
    - chmod 777 ./mvnw
    - ./mvnw clean install
  post_build:
    commands:
    - echo $(basename ./target/*.jar)
    - pwd

cache:
  paths:
  - '/root/.m2/**/*'